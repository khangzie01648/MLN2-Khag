export const useBodyScrollLock = () => {
    // Placeholder hook since we are moving to a no-scroll interactive interface
    // In a real implementation this would lock document.body overflow
    return;
};
